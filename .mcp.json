{
  "mcpServers": {
    "tavily": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.js"],
      "env": {
        "TAVILY_API_KEY": "${user_config.tavily_api_key}"
      }
    }
  }
}
